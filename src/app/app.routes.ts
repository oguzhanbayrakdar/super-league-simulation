import { Routes } from '@angular/router';
import { LeagueComponent } from './core/components/league/league.component';

export const routes: Routes = [
	{
		path: '',
		component: LeagueComponent
	}
];
